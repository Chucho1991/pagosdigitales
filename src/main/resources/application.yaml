server:
  port: ${SERVER_PORT:8080}

cod_GEO_FYB: ${COD_CADENA_GEO_FYB:60}
cod_GEO_SANA: ${COD_CADENA_GEO_SANA:70}
cod_GEO_OKI: ${COD_CADENA_GEO_OKI:90}
cod_GEO_FR: ${COD_CADENA_GEO_FR:70}

spring:
  config:
    import: "optional:file:.env[.properties]"
  application:
    name: pagosDigitales
  datasource:
    url: ${DB_URL:jdbc:oracle:thin:@10.100.3.20:1521:PRS6}
    username: ${DB_USERNAME:TUKUNAFUNC}
    password: ${DB_PASSWORD:tvkvn4tst}
    driver-class-name: ${DB_DRIVER:oracle.jdbc.OracleDriver}
    hikari:
      maximum-pool-size: ${DB_POOL_MAX_SIZE:20}
      minimum-idle: ${DB_POOL_MIN_IDLE:5}
      connection-timeout: ${DB_POOL_CONNECTION_TIMEOUT_MS:2000}
      idle-timeout: ${DB_POOL_IDLE_TIMEOUT_MS:300000}
      max-lifetime: ${DB_POOL_MAX_LIFETIME_MS:1800000}
      data-source-properties:
        oracle.jdbc.timezoneAsRegion: false
#PROVISIONAL DEV
  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none

camel:
  springboot:
    main-run-controller: true

# El mapeo request/response por proveedor se resuelve desde BD:
# TUKUNAFUNC.AD_MAPEO_SERVICIOS
# (APP_SERVICE_KEY, APP_OPERATION, DIRECCION, SECCION_APP/EXT, ATRIBUTO_APP/EXT, ACTIVO)

providers:
  dynamic-routes-enabled: true

safetypay:
  confirmation:
    # Configuracion resuelta desde BD:
    # IN_SAFETYPAY_CFG:
    # CODIGO_BILLETERA, NOMBRE_PROVEEDOR, ENABLED, API_KEY, SECRET, SIGNATURE_MODE, ALLOWED_IPS, ACTIVO
